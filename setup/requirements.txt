# NegLLM — negation geometry experiments
# Versions are lower bounds; the stack was last verified with the versions in brackets.

# --- core numerics -----------------------------------------------------------
numpy>=1.26
pandas>=2.0
pyarrow>=14.0          # parquet I/O for the minimal-pair datasets

# --- model stack -------------------------------------------------------------
torch>=2.2             # [2.14.0]  MPS is used automatically on Apple Silicon
transformers>=4.40     # [5.16.1]  v5 changed get_text_features' return type; both APIs are handled

# --- analysis & figures ------------------------------------------------------
matplotlib>=3.8
scikit-learn>=1.3      # linear probe + cross-validation

# --- notebook runtime --------------------------------------------------------
ipykernel>=6.29
jupyterlab>=4.0
